"use strict";
{ //interface
    //diff 1 : Primitive data type can be defined by type Alias but can't be defined by Interface
    const user1 = {
        name: "tajbid",
        age: 21,
        role: "student"
    };
    const user2 = {
        name: "Tajbid",
        age: 21,
        role: "student"
    };
    const rollNumber1 = [1, 2, 3, 5, 6];
    const rollNumber2 = [1, 2, 3, 5, 6];
    const add1 = (a, b) => a + b;
    const add2 = (a, b) => a + b;
}
